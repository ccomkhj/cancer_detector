# Requirements for 2.5D Segmentation Pipeline
# 
# Install with: pip install -r requirements_2d5.txt
#
# Note: This includes everything from requirements.txt plus deep learning packages

# ============================================================================
# Core MRI Processing (from original requirements.txt)
# ============================================================================
pandas>=1.5.0
pyarrow>=10.0.0
openpyxl>=3.0.0
numpy>=1.24.0
pillow>=9.4.0
pydicom>=2.3.0
SimpleITK>=2.2.0
trimesh>=3.20.0
tqdm>=4.64.0

# Optional for advanced DICOM processing
highdicom>=0.18.0

# ============================================================================
# Deep Learning for 2.5D Segmentation (NEW)
# ============================================================================

# PyTorch (choose based on your CUDA version)
# CPU-only:
torch>=2.0.0
torchvision>=0.15.0

# For CUDA 11.8:
# torch>=2.0.0+cu118
# torchvision>=0.15.0+cu118

# For CUDA 12.1:
# torch>=2.0.0+cu121
# torchvision>=0.15.0+cu121

# Medical Imaging AI Framework
monai>=1.3.0

# Segmentation Models Library
segmentation-models-pytorch>=0.3.3

# Additional utilities
scikit-learn>=1.2.0
matplotlib>=3.7.0
seaborn>=0.12.0
loguru
wandb 
weave

# ============================================================================
# Installation Instructions
# ============================================================================
#
# 1. For CPU-only (testing/development):
#    pip install -r requirements_2d5.txt
#
# 2. For GPU with CUDA 11.8:
#    pip install torch==2.0.0+cu118 torchvision==0.15.0+cu118 --index-url https://download.pytorch.org/whl/cu118
#    pip install -r requirements_2d5.txt
#
# 3. For GPU with CUDA 12.1:
#    pip install torch==2.0.0+cu121 torchvision==0.15.0+cu121 --index-url https://download.pytorch.org/whl/cu121
#    pip install -r requirements_2d5.txt
#
# 4. Check installation:
#    python -c "import torch; print(f'PyTorch: {torch.__version__}')"
#    python -c "import monai; print(f'MONAI: {monai.__version__}')"
#    python -c "import segmentation_models_pytorch as smp; print(f'SMP: {smp.__version__}')"

