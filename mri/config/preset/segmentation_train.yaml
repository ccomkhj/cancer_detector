task:
  name: segmentation
  stage: train

experiment:
  purpose: segmentation

train:
  batch_size: 4
  epochs: 100
  lr: 5e-5
  weight_decay: 1e-4
  output_dir: checkpoints/seg

loss:
  name: dice_bce

metrics:
  names: [dice]
  segmentation_threshold: 0.5
